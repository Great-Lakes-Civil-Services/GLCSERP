<!-- Views/LoginView.xaml -->
<UserControl x:Class="CivilProcessERP.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="800" Width="1400">
    <Grid Background="#F0F0F0">
        <Border Width="400" Height="300" CornerRadius="10" Background="White" Padding="20"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="User Login" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>
                <TextBox x:Name="UsernameBox" Height="35" Margin="0,0,0,10"/>
                <PasswordBox x:Name="PasswordBox" Height="35" Margin="0,0,0,20"/>
                <Button Content="Login" Height="40" Click="Login_Click" Background="#007272" Foreground="White"/>
                <TextBlock x:Name="LoginError" Foreground="Red" FontSize="12" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

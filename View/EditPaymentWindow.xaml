<Window x:Class="CivilProcessERP.Views.EditPaymentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Payment" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Payment Date:" Grid.Row="0" Margin="5"/>
        <TextBox Grid.Row="0" Margin="120,5,5,5" Text="{Binding Date}" />

        <TextBlock Text="Method:" Grid.Row="1" Margin="5"/>
        <TextBox Grid.Row="1" Margin="120,5,5,5" Text="{Binding Method}" />

        <TextBlock Text="Amount:" Grid.Row="2" Margin="5"/>
        <TextBox Grid.Row="2" Margin="120,5,5,5" Text="{Binding Amount}" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Width="80" Margin="5" Click="Save_Click"/>
            <Button Content="Cancel" Width="80" Margin="5" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>

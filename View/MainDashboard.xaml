<UserControl x:Class="CivilProcessERP.Views.MainDashboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="720" Width="1280">
    <Grid Background="#F5F5F5">

        <!-- ✅ Main Content Area -->
        <Grid Margin="30">
            <StackPanel>
                <TextBlock Text="Welcome to your Dashboard" 
                           FontSize="28" FontWeight="Bold" Foreground="Black" 
                           Margin="10,10,0,15"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>

                    <!-- ✅ Notification Panel -->
                    <Border Grid.Column="0" Width="300" Height="150" Background="White" 
                            CornerRadius="8" Margin="10" Padding="15" VerticalAlignment="Top">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" Opacity="0.2" ShadowDepth="5"/>
                        </Border.Effect>
                        <StackPanel>
                            <Image Source="/Assets/alert.png" Height="30"/>
                            <TextBlock Text="Notifications" FontSize="18" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>

                    <!-- ✅ Search Box with Placeholder -->
                    <Grid Grid.Column="1" Height="50" Width="350" Margin="10" VerticalAlignment="Top">
                        <Border Background="White" BorderBrush="Gray" BorderThickness="1" 
                                CornerRadius="5" Padding="5">
                            <Grid>
                                <!-- Search Box -->
                                <TextBox x:Name="SearchBox"
                                         Height="40"
                                         FontSize="14"
                                         VerticalAlignment="Center"
                                         Padding="10"
                                         Foreground="Black"
                                         KeyDown="SearchBox_KeyDown"
                                         Background="Transparent"
                                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>

                                <!-- Placeholder Text (Simulated) -->
                                <TextBlock Text="Enter Job Number..."
                                           Foreground="Gray"
                                           Margin="15,0,0,0"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </StackPanel>
        </Grid>

    </Grid>


</UserControl>

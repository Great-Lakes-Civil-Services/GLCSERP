<UserControl x:Class="CivilProcessERP.Views.DeleteUserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    
    <Grid Background="WhiteSmoke" Margin="20">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
            <TextBlock Text="Delete User" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>
            
            <TextBlock Text="Username" />
<ComboBox x:Name="UsernameSearchComboBox"
          IsEditable="True"
          Margin="0,5,0,20"
          KeyUp="UsernameSearchComboBox_KeyUp"
          DisplayMemberPath="LoginName"
          HorizontalContentAlignment="Stretch"/>


            <TextBlock Text="⚠ This action is irreversible." Foreground="Red" FontStyle="Italic" Margin="0,0,0,10"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="Confirm Delete" Click="ConfirmDeleteButton_Click" Width="140" Background="DarkRed" Foreground="White" Margin="10"/>
                <Button Content="Cancel" Click="CancelButton_Click" Width="100" Background="Gray" Foreground="White"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>

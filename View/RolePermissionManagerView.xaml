<UserControl x:Class="CivilProcessERP.Views.RolePermissionManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="WhiteSmoke">

    <StackPanel Margin="20">
        <TextBlock Text="Role Permission Manager" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Select Role:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="RoleComboBox" Width="300" SelectionChanged="RoleComboBox_SelectionChanged"/>
        </StackPanel>

        <ListBox x:Name="PermissionsListBox" SelectionMode="Multiple" Margin="0,20,0,0" Height="300"/>

        <Button Content="Save Permissions" Click="SavePermissions_Click"
                Width="200" Margin="0,10,0,0"
                Background="DarkGreen" Foreground="White"/>

                <Button Content="Cancel"
        Click="Cancel_Click"
        Width="200"
        Margin="0,10,0,0"
        Background="Gray"
        Foreground="White"/>

    </StackPanel>
</UserControl>

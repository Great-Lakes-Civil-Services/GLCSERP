<UserControl x:Class="CivilProcessERP.Views.AssignPermissionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="WhiteSmoke" Margin="20">
        <StackPanel>
            <TextBlock Text="Assign Permissions" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>
            
            <TextBlock Text="Select User:"/>
            <ComboBox x:Name="UserComboBox"
                      Width="300"
                      Margin="0,5"
                      SelectionChanged="UserComboBox_SelectionChanged"/>

            <TextBlock Text="Select Permissions:" Margin="10,10,0,5"/>
            <ListBox x:Name="PermissionListBox"
                     Height="200"
                     SelectionMode="Multiple"/>

            <Button Content="Save Permissions"
                    Width="150"
                    Background="Teal"
                    Foreground="White"
                    Margin="0,20,0,0"
                    Click="SavePermissions_Click"/>

            <Button Content="Cancel"
        Width="150"
        Background="Gray"
        Foreground="White"
        Margin="0,10,0,0"
        Click="CancelButton_Click"/>        
        </StackPanel>
    </Grid>
</UserControl>
